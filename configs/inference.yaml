_base_: base.yaml

model:
  channels: 128
  num_blocks: 12
  policy_channels: 64
  value_channels: 32

inference:
  temperature: 0.0
  top_k: 3
  device: auto  # auto / cuda / cpu
  checkpoint: checkpoints/best.pt
