_base_: base.yaml

model:
  channels: 192
  num_blocks: 10
  policy_channels: 96
  value_channels: 48

inference:
  temperature: 0.0
  top_k: 3
  device: auto  # auto / cuda / cpu
  checkpoint: checkpoints/best.pt
