_base_: base.yaml

model:
  channels: 96
  num_blocks: 8
  policy_channels: 48
  value_channels: 24

inference:
  temperature: 0.0
  top_k: 3
  device: auto  # auto / cuda / cpu
  checkpoint: checkpoints/best.pt
